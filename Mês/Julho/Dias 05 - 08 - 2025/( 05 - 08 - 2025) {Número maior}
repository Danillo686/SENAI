let maior = 0;
let numero;


  numero = Number(prompt("Digite um número inteiro positivo (-1 para encerrar):"));

  if (numero > maior) {
    maior = numero;
   }

   while (numero !== -1);

   alert("O maior número digitado foi: " + maior);
